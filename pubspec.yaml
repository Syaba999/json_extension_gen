name: json_extension_gen
description: Generate extension for JSON factory and toJson
version: 0.0.1
author: Syaba999
homepage: http://naoplus.ru

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  analyzer: ^0.39.8
  build: ^1.3.0
  source_gen: ^0.9.5
  json_serializable: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0